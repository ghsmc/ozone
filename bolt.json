{
  "name": "milo-ai-discovery",
  "description": "AI Discovery Engine for Yale Students",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build",
    "dev": "npm run dev:full",
    "postbuild": "cp server.js dist/ && cp -r node_modules dist/ && cp package.json dist/"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "build": {
    "command": "npm run build && npm run postbuild",
    "output": "dist",
    "functions": "server.js"
  },
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
